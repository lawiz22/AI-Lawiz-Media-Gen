import React from 'react';

// This component is intentionally left blank.
// The application's design requires the API key to be set via environment variables (process.env.API_KEY)
// and explicitly forbids prompting the user for it in the UI.
// This empty component prevents accidental use of an anti-pattern.

export const ApiKeyModal: React.FC = () => {
  return null;
};
