import type { MannequinStyle } from '../types';

// This file is now intentionally left without exports for mannequin styles,
// as the application has been updated to only use the 'custom-reference'
// method, which requires a user-uploaded image instead of these static assets.
